name: rick_and_morty
description: A Rick and Morty Explorer.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  easy_debounce: ^2.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^7.0.1
  flutter_easyloading: ^3.0.0
  get: ^4.1.4
  lottie: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/lottie/
